'use strict';

/**
 * registration-pending-email router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::registration-pending-email.registration-pending-email');
